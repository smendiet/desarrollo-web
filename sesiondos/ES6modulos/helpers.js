// Ejemplo de export en linea

// export const sum = (a, b) => a + b;
// export const multiply = (a, b) => a * b;

// Ejemplo de export en General

const sum = (a, b) => a + b;
const multiply = (a, b) => a * b;

export { sum, multiply };
